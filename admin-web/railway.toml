[build]
builder = "DOCKERFILE"
dockerfilePath = "admin-web/Dockerfile"
watchPatterns = ["src/**", "admin-web/**", "requirements.txt"]

[deploy]
healthcheckPath = "/healthz"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
