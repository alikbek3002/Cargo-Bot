[build]
builder = "DOCKERFILE"
dockerfilePath = "worker/Dockerfile"
watchPatterns = ["src/**", "worker/**", "requirements.txt", "example_adress.txt"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
